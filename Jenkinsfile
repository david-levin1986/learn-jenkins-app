pipeline {
    agent any

    stages {
        stage('test docker alpine') {
            steps {
                echo 'testing docker alpine'
            }
            steps {
                echo 'testing docker microsoft'
            }
        }
        
    }
}
